#include <stdio.h>
#include <stdlib.h>

#include "ft_abs.h"
int main()
{
  int x;
  int y;

  x = ABS(3);
  printf("absolutes value of x = %d\n", x);
  y = ABS(-1);
  printf("absolute value of y = %d\n", y);
  return (0);
}
